function downloadFiles(files)
{
     for (i=0;i<files.length;i++)
     {
          //var img = $('<img>');
          //img.attr('src', "images/" + files[i]);
        //#img.appendTo('#content');
     }
}