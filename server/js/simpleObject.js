var Entity = require('./entity'),	Constants = require('../../client/js/constants');module.exports = SimpleObject = Entity.extend({    init: function(id ,object, engine) {    	this._super(id, object.type, Types.Entities[object.type]);		this.setPosition(object.x, object.y);		this.animation = "basic";		this.body = engine.createBody(object.x, object.y);		if (object.type !== "border")			this.body.m_userData = this;		if (object.width == 0 && object.height == 0){		  //this.fixture = engine.createPolygonFixture(this.body, object.polyline);		}		else{		  this.fixture = engine.createBoxFixture(this.body, object.width, object.height);		  engine.addEntity(this);		}    },});