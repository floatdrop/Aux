var Entity = require('./entity'),	Constants = require('../../client/js/constants');module.exports = CommonEntity = Entity.extend({	init: function(id ,object, engine) {    	this._super(id, object.type, Types.Entities.CommonEntity);		var width = object.width / 100;		var height = object.height / 100;		var x = object.x / 100;		var y = object.y / 100;		this.setPosition(x, y);		this.animation = "basic";		//console.log(x + " " + y + " " + object.type + " " + width + " " + height);		// if (object.type !== "border" && object.type !== "stump"&& object.type !== "trie")			// this.body.m_userData = this;		if (object.width == 0 && object.height == 0){		  //this.fixture = engine.createPolygonFixture(this.body, object.polyline);		}		else{			// if (object.type === "border" || object.type === "stump" || object.type === "tsrie")				// return;			console.log(x + " " + y + " " + object.type + " " + width + " " + height);			this.body = this.createBody(engine.b2w, x, y);			this.body.m_userData = this;			this.fixture = this.createBoxFixture(this.body, width, height);			engine.addEntity(this);		}    },		getBaseState: function() {        return {            id: this.id,            kind: this.kind,            position: this.getPosition(),            angle: this.getAngle(),            animation: this.animation,			sprite: this.type        };    },});